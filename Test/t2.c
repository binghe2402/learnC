#include <stdio.h>
int main()
{
    //FILE *fp;
    //fp = fopen("soft_function0000t.txt","r");
    //int a, b, c, d, e, f, g, h;
    //double i, j;
    double ss[3];
    //fscanf(fp,"%lf", &a);
    //fscanf(fp,"%d %d %d %d %d %d %d %d %lf %lf\n",&a,&b,&c,&d,&e,&f,&g,&h,&i,&j);

    printf("%d", sizeof(ss));

    return 0;
}
